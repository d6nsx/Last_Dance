import React from 'react';
import Registration from './components/Registration';

const App = () => {
  return React.createElement('div', null, React.createElement(Registration));
};

export default App;
